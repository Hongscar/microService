server:
  port: 8080
spring:
  application:
    name: configserver
  cloud:
      config:
        server:
          git:
            uri: https://gitee.com/itmuch/spring-cloud-config-repo
            # https://gitee.com/itmuch/spring-cloud-config-repo
            # https://github.com/Hongscar/microService
            search-paths: config-repo
            username:
            password:
          default-application-name: configserver
#consul:
#  host: consul
#  port: 8500
#  enabled: true
#  discovery:
#    enabled: true
#    register: true
#    service-name: configserver